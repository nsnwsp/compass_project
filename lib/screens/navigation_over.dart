import 'package:flutter/material.dart';

class NavigatioOver extends StatelessWidget {
  const NavigatioOver({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Hey è finita"),
        ),
      ),
    );
  }
}
